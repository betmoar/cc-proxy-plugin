# AUDIT_LOG.md — append-only

- 2026-08-31 P1: mode=AUTONOMOUS, target=/home/user/cc-proxy-plugin @ a6f05ab (branch claude/principal-audit-autonomous-fd05gq). No prior cursor.
- 2026-08-31 P1: OPERATOR.md charter tooling (.operator/) absent in checkout — logged as charter conflict; proceeding under audit skill evidence discipline.
- 2026-08-31 P1: read src/*.js (all 11), hooks/* (all), bin/cc-proxy.js, scripts/{status,statusline,quota,start-proxy,render-html,release-gate,sync-version,list-models}, skills/setup/SKILL.md, .github/workflows/*, plugin.json.
- 2026-08-31 P1: BASELINE `pnpm check` GREEN: lint clean; 469 tests / 467 pass / 0 fail / 2 skipped (env-gated live-key tests). capture_baseline.sh: "npm test present".
- 2026-08-31 P1: EXIT CRITERIA met (arch map, load-bearing map w/ blast radius, implicit contracts, delta) → AUDIT_STATE.md. Advancing to P2.
- 2026-08-31 P2: not yet read (to sweep): scripts/render-models.js, bench-grades.js, bench-speed.js, probe-vendors.mjs, commands/*.md, docs/*.md, test/couplings.test.js. Candidate probes queued: IC9 client-abort 'error' race; statusline lock reclaim race; coupling ⚠ rows.
- 2026-08-31 P2: probe IC9 (streaming client-abort race): 60 aborts across 2-31ms timing window against real createServer + slow SSE stub — zero unhandled errors/rejections. Streaming teardown CONFIRMED sound (scratchpad/abort-probe.mjs).
- 2026-08-31 P2: swept scripts/{render-models,bench-speed,bench-grades(head),probe-vendors(head),quota}.js, commands/*.md, .env.example, workflows, couplings.test.js. Wire-shape docs (README/OPERATIONS/ARCHITECTURE) verified in sync with ModelEntry fields. Media tunnel + GAUGES + lock single-flight have real test coverage.
- 2026-08-31 P2: findings follow (schema per finding-schema.md).

### [F01] Host header sent upstream drops a non-default port
- **Location:** src/providers.js:466 (`headers.host = hostname`), fed from src/proxy.js:131-137 (`upstreamRequestOptions` passes `url.hostname`)
- **Severity:** P2
- **Confidence:** high
- **Claim tag:** CONFIRMED — reproduced: proxy with LMSTUDIO_BASE_URL=http://127.0.0.1:36479 forwarded `lmstudio:` request; upstream received `Host: 127.0.0.1` (scratchpad/host-probe.mjs output: "DEFECT: port dropped from Host header")
- **Failure trigger:** any provider whose baseUrl carries a non-default port — today exactly LM Studio, whose documented setup form is `http://192.168.1.50:1234`. Every forwarded request sends `Host: 192.168.1.50` (RFC 9112 §3.2 requires host:port for non-default ports).
- **Blast radius:** silent. LM Studio itself currently tolerates it, but any name/port-based front (reverse proxy, vhost router, tunnel) misroutes or 421/400s, and the request looks fine at the cc-proxy log. Hardcoded vendors are unaffected (default ports; `url.host === url.hostname` there).
- **Evidence:** probe above; `new URL("http://127.0.0.1:36479").host` = "127.0.0.1:36479" vs `.hostname` = "127.0.0.1".
- **Fix:** pass `url.host` (not `url.hostname`) into buildUpstreamHeaders; existing test `h.host === "api.z.ai"` stays green because https default port renders no port in `url.host`.
- **Guardrail:** end-to-end test asserting the upstream-received Host header carries the port for a port-carrying baseUrl (server.test.js, both paths share upstreamRequestOptions so one suffices + a unit assert).

### [F02] statusline refresh-lock stale reclaim is check-then-write — two renders can both reclaim
- **Location:** scripts/statusline.js:271-275 (statSync mtime check, then plain writeFileSync)
- **Severity:** P3
- **Confidence:** moderate
- **Claim tag:** CONFIRMED (traced) — the reclaim branch takes no exclusive flag; two processes that both observe age >= REFRESH_LOCK_STALE_MS both return true. Race itself not reproduced (sub-ms window).
- **Failure trigger:** a refresher killed >10s ago left refresh.lock; two ~300ms renders race the reclaim within the same tick window.
- **Blast radius:** bounded and non-corrupting: two detached refreshers spawn, each re-checks cache freshness per gauge, so at worst one duplicate round of quota API calls. Fails silent but self-heals.
- **Evidence:** scripts/statusline.js lines 263-280 read in full; single-flight comment documents the wx path only.
- **Fix:** reclaim via `rmSync(lockPath, {force:true})` then retry the exclusive `wx` create once; losers return false.
- **Guardrail:** unit test: abandoned (backdated-mtime) lock present → exactly one of two sequential takeRefreshLock calls wins after reclaim (the second sees a FRESH lock and returns false).

### [F03] streaming 429-peek answers an upstream read error with a socket hang-up where the buffered path sends 502
- **Location:** src/proxy.js:207 (`upstreamRes.on("error", () => clientRes.destroy())`)
- **Severity:** P3
- **Confidence:** moderate
- **Claim tag:** CONFIRMED (traced) — before headersSent the destroy yields a client-visible connection reset; src/server.js:252-255 (forwardBuffered) handles the identical failure with a 502 JSON error when headers are unsent. Not reproduced against a live socket.
- **Failure trigger:** upstream returns 429 on a stream:true request, then the socket dies mid-error-body (vendor reset, timeout destroy).
- **Blast radius:** loud but wrong shape: the client sees ECONNRESET instead of a 502 explaining the upstream failed; retry/backoff logic keyed on status codes never sees one. Rare path (429 + mid-body death).
- **Evidence:** the two handlers read side-by-side; error contract divergence between the paths is the exact drift class onUpstreamError was extracted to prevent (its own docstring).
- **Fix:** mirror forwardBuffered: `if (clientRes.headersSent) clientRes.destroy(); else sendJson-like 502`.
- **Guardrail:** server.test.js case: stub 429 that destroys its socket mid-body on a stream:true request → client must receive HTTP 502, not a reset.
- 2026-08-31 P2: EXIT — dry pass reached; nothing above P3 remains. TRUNCATION LOGGED: bench-grades.js read to ~line 120, probe-vendors.mjs to ~line 80 (manual tools, own test files); docs/BACKLOG.md read structurally, not line-by-line.
- 2026-08-31 P3: F01+F03 tests written FIRST, both red against unfixed code (`not ok 1/2`, # fail 2) — defects reproduced at the test gate.
- 2026-08-31 P3: F01 fixed (src/proxy.js url.host; providers.js param host + JSDoc). F03 fixed (429 branch → onUpstreamError). F02 fixed (statusline reclaim → rmSync .stale / renameSync takeover / wx re-entry).
- 2026-08-31 P3: VERIFY — new tests green (`ok 1/2`, # pass 2); full `pnpm check` GREEN: 471 tests / 469 pass / 0 fail / 2 skipped (baseline 467 pass / 0 fail → +2, no regressions). Statusline reclaim/hold tests included and green.
- 2026-08-31 P3: EXIT — every finding fixed & verified or structurally argued (F02 race-test DECISION in AUDIT_STATE.md).
- 2026-08-31 P4: guardrails — couplings.test.js gains "every bare static-catalog id has a usable ROUTES entry" and "every published /v1/models field is named in README/OPERATIONS/ARCHITECTURE" (fields parsed from the ModelEntry typedef). Ran: 15 pass / 0 fail.
- 2026-08-31 P4: context transfer — CLAUDE.md couplings table updated (two ⚠ rows now name their locks); publishing-contract paragraph updated; CHANGELOG [0.8.1] unreleased section written (3 Fixed, 1 Changed).
- 2026-08-31 P4: FINAL GATE `pnpm check` (post-docs edits): 473 tests / 471 pass / 0 fail / 2 skipped, lint clean. AUDIT_STATE.md rewritten to cursor DONE with verdict, remediation table, residual-risk register, decisions.
- 2026-08-31 CI: first CI run on PR #50 head 32ecd82 failed — 1 test: start-proxy.test.js "falls back to settings.json PROXY_PATH when the tree has no bin" (3082ms ≈ full readiness timeout, empty stdout). Diff-unrelated; root-caused below.

### [F04] start-proxy tests race concurrent test files for the freePort() port — stand-in bin dies on EADDRINUSE
- **Location:** test/start-proxy.test.js:24 (standinBin, no 'error' handler) + :12 (freePort releases the port before the spawn uses it)
- **Severity:** P2
- **Confidence:** high
- **Claim tag:** CONFIRMED — reproduced deterministically (scratchpad/port-steal-probe.mjs): occupy the freed port before the spawn → stand-in exit 1, "listen EADDRINUSE", flag never written; matches CI's empty-stdout assertion failure and its ~3000ms duration (readiness poll exhausting).
- **Failure trigger:** node --test runs test FILES concurrently; another file's listen(0) is assigned the ephemeral port freePort() just released, in the gap before the spawned stand-in binds it.
- **Blast radius:** flaky CI red on an untouched test — erodes trust in the gate and burns re-run cycles; fails loud but misattributes (looks like a resolution bug).
- **Evidence:** probe output above; CI job 99362121103 log: "not ok 3 … Expected started, got: ''", duration_ms 3082.
- **Fix:** stand-in retries listen on EADDRINUSE (bounded, 50×100ms — the thief is an ephemeral test socket); per-run PROXY_READY_TIMEOUT_MS=8000 in the test harness env for headroom. Shipped defaults untouched.
- **Guardrail:** the retry IS the guardrail (a wrong-bin regression still never writes the right flag, so test semantics are preserved); validated 5 consecutive green runs + full suite green.
- 2026-08-31 CI: author pushed 9cc09d5 (release 0.8.1 prep, green) then 6698dc7 — a rework of the F02 fix into scripts/refresh-lock.js with an afterStat test seam + 3 deterministic tests, verifying the moved file by INODE. Its own race test failed on CI (ubuntu) and locally while passing the author's APFS measurement.

### [F05] refresh-lock inode verification false-matches on inode-recycling filesystems — double-grant returns on Linux
- **Location:** scripts/refresh-lock.js:76 (`statSync(claimed).ino !== seen.ino` as the sole identity check)
- **Severity:** P2
- **Confidence:** high
- **Claim tag:** CONFIRMED — probe on this machine (same image class as CI): rm then wx-create returned the IDENTICAL ino (1884209 → 1884209, "REUSED"); the author's own test "hands an abandoned lock to exactly ONE of two racing reclaimers" was red here and on CI (first=true) and is green on APFS where inode numbers are never reused.
- **Failure trigger:** loser judged the lock stale; winner relocks (rm frees inode, wx-create gets the SAME inode back on ext4/overlayfs); loser renames the fresh lock away, its ino comparison matches, loser also wins.
- **Blast radius:** the exact double-grant the rework set out to close, deterministic on Linux (the platform CI and most users run); silent duplicate refresher + quota fetches.
- **Evidence:** ino-reuse probe output; local test run red pre-fix (`not ok 1 … true !== false`), green post-fix; CI job 99377745487.
- **Fix:** identity = ino AND mtimeMs. rename preserves mtime; a takeover lock is a fresh write stamped now, while a judged-stale file's mtime is ≥10s old, so the pair can never coincide. CHANGELOG + code + test comments corrected (they asserted the inode-only contract).
- **Guardrail:** the author's deterministic seam test IS the guardrail — it fails the inode-only variant on any inode-recycling filesystem; comments now state the platform dependence so the APFS-only measurement cannot re-justify the weaker check.
- 2026-08-31 CI: VERIFY — lock suite 3/3 green; full `pnpm check` GREEN: 476 tests / 474 pass / 0 fail / 2 skipped, lint clean.
- 2026-08-31 REVIEW: Copilot review on PR #50, 5 comments. Verified each: (1) three-contender reclaim hole CONFIRMED by reading (loser holds winner's renamed-away lock, path empty, fast-path wx wins, restore clobbers) → reclaimers now SERIALIZE on refresh.lock.claim (exclusive wx + stale recovery), re-validate staleness under the claim, and restore via link() which atomically refuses an existing destination; flock declined (native dep, zero-dep repo); residue (hung-refresher unlink inside µs windows) documented in the module header. 3 new seam tests (afterRestat/afterRename hooks); the clobber test mutation-verified RED against a rename-back restore, green with link(). (2) CLAUDE.md trap bullet still asserted inode-only → corrected to ino+mtime + ceremony. (3) couplings typedef parser scope → deliberate (docs describe cc-proxy's OPTIONAL additions; required four are Anthropic's base shape) — comment now states it; replying on thread. (4)(5) AUDIT_STATE stale DECISION + totals → updated (decision marked SUPERSEDED by the author's seam; final figures 479/477/0).
- 2026-08-31 REVIEW: VERIFY — lock suite 6/6 green; full `pnpm check` GREEN: 479 tests / 477 pass / 0 fail / 2 skipped, lint clean.
- 2026-08-31 REVIEW-2: Copilot round 2, 2 comments, both valid. (1) The stale-CLAIM recovery (rm+recreate) was the same check-then-act race one level down — one racer could delete the other's fresh claim and both ran the ceremony. Fixed: ONE shared takeover ceremony (takeStale: judge-by-mtime → rename-to-pid-private → verify ino+mtime → link()-restore on mismatch) now serves both the lock and the claim; claim recovery goes through it. New deterministic test "hands an abandoned CLAIM to exactly ONE of two racing reclaimers" (claim.afterRestat seam) — 7 lock tests total; clobber test re-mutation-verified red under a rename-back restore. (2) CHANGELOG 0.8.1 lock paragraph described the superseded round-2 protocol — rewritten to the full defect ladder + shipped protocol + stated residue. Convergence note: if a further round re-flags the flock-free residue itself, stop pushing and raise once (it is stated as irreducible without a native dep).
- 2026-08-31 REVIEW-2: process slip logged: a `git checkout` used to restore the module after a mutation run resurrected the COMMITTED (older) version over the uncommitted rewrite — caught immediately by the test suite (claim test red), rewritten from context, all green. Destination check (charter SOLO rule) would have caught it pre-run.
- 2026-08-31 REVIEW-2: VERIFY — lock suite 7/7; full `pnpm check` GREEN: 480 tests / 478 pass / 0 fail / 2 skipped, lint clean.
- 2026-08-31 REVIEW-3: Copilot round 3 on 9d230e6 — verdict softened (🟡→🔵), ZERO new comments, 2 suppressed refinement notes on test infrastructure. CONVERGED; this is the last round driven from this side. (a) start-proxy port race, second shape ("already up" when a thief still LISTENS at probe time — distinct from the EADDRINUSE shape F04 fixed): fixture-level retry added — runWithPortRetry re-runs once on a freshly allocated port; a real resolution regression reproduces on both ports, a collision does not follow to a new one. 3 consecutive green runs. (b) whole-document token search could false-pass a hypothetical common-word field: limitation now STATED in the test comment with the sharper variant named (section-scoping), deliberately not built today (three hand-written docs, three heading styles — the parser would be brittler than the drift it guards).
- 2026-08-31 REVIEW-3: VERIFY — full `pnpm check` GREEN: 480 tests / 478 pass / 0 fail / 2 skipped, lint clean. No further Copilot re-request from this side (convergence rule); the author can request another round if wanted.
- 2026-09-02 P1: RUN 2 start. mode=AUTONOMOUS, target=/home/user/cc-proxy-plugin @ f28fa27 (branch claude/principal-audit-autonomous-wgn520). Prior cursor DONE @ a6f05ab → this run audits the delta (#50, #53), the previously-truncated script tails, and re-reads every load-bearing path. Finding ids continue from F06.
- 2026-09-02 P1: charter conflict (unchanged): .operator/ tooling absent; proceeding under the audit skill's evidence discipline. COMMIT gate treated as cleared for THIS branch + issue creation on the task statement's explicit words (logged in AUDIT_STATE.md).
- 2026-09-02 P1: BASELINE `pnpm check` GREEN: lint clean; 504 tests / 502 pass / 0 fail / 2 skipped (node v22.22.2, pnpm 11.3.0, deps installed via `pnpm install --frozen-lockfile`).
- 2026-09-02 P1: read in full: src/*.js (11), hooks/*, bin/cc-proxy.js, scripts/{proxy... statusline,refresh-lock,version-guard,status,start-proxy,quota,release-gate,render-html,sync-version,list-models,probe-vendors,bench-speed,bench-grades}, skills/setup/SKILL.md, commands/*.md, .github/workflows/*, .forgejo/workflows/gate.yml, .npmrc, .env.example, docs/BACKLOG.md, test/{couplings,doc-examples}.test.js, CHANGELOG 0.7.0–0.8.2. TRUNCATION LOGGED: scripts/render-models.js 1–679 not re-read (prior run swept it); README/OPERATIONS/ARCHITECTURE grepped, not line-read.
- 2026-09-02 P1: probe (scratchpad/with space/): status.js and list-models.js copied under a directory named "with space" and invoked directly → EMPTY stdout, exit 0 for both. The guard compares `file:///…/with space/…` (argv form) against `file:///…/with%20space/…` (import.meta.url). CONFIRMED defect → F06.
- 2026-09-02 P1: probe (scratchpad/probe-hang.mjs): stub /_status writes headers + partial body then destroys the socket; `probeProxyVersion(port)` STILL PENDING after 2500ms (Promise.race). No 'end', no uncaught 'error' (scratchpad/res-error-probe.mjs: process survived, "end" never fired). CONFIRMED hang → F07.
- 2026-09-02 P1: EXIT CRITERIA met (map re-verified + one addition, IC10–IC13, delta) → AUDIT_STATE.md rewritten with cursor P2.
- 2026-09-02 P2: swept correctness / security / robustness / quality / gaps / footguns over the P1 read set. Confirmed sound, no finding (where NOT to spend effort): the five `writeHead` sites all pass `withoutRequestId`; `applyAuth` drops both inbound credential headers; `push()` grouping in collectModels (traced 5 shapes by hand); `rankRoutes` native-first sort; `refresh-lock.js` ceremony (unchanged since review round 3); `version-guard.js` fail-closed on a missing `ps`/git; `release-gate.mjs`; `render-html.mjs` HOME isolation; the 429/1302 gate; `spawnProxy` fd handling. Inbound request body is unbounded — only reachable off-loopback, so it belongs with issue #45, not here (noted for P4).
- 2026-09-02 P2: probe (scratchpad/abort-midbody.mjs): stub GLM /v1/models sends headers + `{"data":[` and stalls; `collectModels` with modelsTimeoutMs=200 reports `{"provider":"glm","message":"invalid response shape"}` — the abort landed inside the body read and was mislabelled. CONFIRMED → F10.
- 2026-09-02 P2: findings follow (schema per finding-schema.md).

### [F06] The direct-run guard in five scripts is not URL-encoded, so a path with a space (or any symlink, or Windows) makes them silent no-ops
- **Location:** scripts/status.js:189, scripts/list-models.js:225, scripts/bench-speed.js:271, scripts/bench-grades.js:405, scripts/render-models.js:705 — all `if (import.meta.url === \`file://${process.argv[1]}\`)`
- **Severity:** P2
- **Confidence:** high
- **Claim tag:** CONFIRMED — reproduced (scratchpad/with space/): `node "$PWD/scripts/status.js"` and `node "$PWD/scripts/list-models.js"` from a directory named `with space` print NOTHING and exit 0; the two sides compare `file:///…/with space/…` vs `file:///…/with%20space/…`
- **Failure trigger:** the plugin tree (or the user's `$HOME`) sits under a path containing a space, `%`, `#` or `?`; OR the path is reached through a symlink (Node resolves the main module's `import.meta.url` to the realpath, `argv[1]` stays the symlink); OR the platform is Windows, where `file://C:\…` never equals `file:///C:/…`. `/cc-proxy:status`, `/cc-proxy:models`, `/cc-proxy:bench` and `pnpm models:html` all route through these five scripts.
- **Blast radius:** SILENT. `main()` never runs; the process exits 0 with empty stdout. `commands/status.md` then tells the user "the proxy may be down" — a wrong diagnosis for a path-encoding bug. On Windows this is every operator command, every time (README:343 says "Windows untested"; this is one concrete reason it does not work). The three sibling scripts that use the robust spelling (`probe-vendors.mjs`, `release-gate.mjs`, `version-guard.js`) show the repo already knew the right form and did not sweep the class — the same "fix two of three sites" lesson `logSafe()`'s comment records.
- **Evidence:** repro above; `pathToFileURL("/x/with space/s.js").href` → `file:///x/with%20space/s.js`. Also `test/config.test.js` and `test/status.test.js` import these modules and never exercise the guard, so no test could have caught it.
- **Fix:** one shared `scripts/direct-run.js` exporting `isDirectRun(import.meta.url)` that compares `fs.realpathSync(path.resolve(argv[1]))` with `fs.realpathSync(fileURLToPath(metaUrl))` (decodes percent-escapes, resolves symlinks, handles Windows drive paths); replace the literal in all five scripts.
- **Guardrail:** (a) `test/direct-run.test.js` — unit cases for space, symlink, foreign entry, missing argv; plus an end-to-end run of `scripts/status.js` through a symlinked path containing a space that must print the DOWN report. (b) `test/couplings.test.js` lock: no file under src/scripts/hooks/bin may spell the guard as `import.meta.url === \`file://…` — the class is forbidden, not the five instances.

### [F07] `probeProxyVersion()` and `requestShutdown()` never settle when the response is cut mid-body, so the SessionStart hook hangs until its 10 s kill
- **Location:** hooks/proxy-lifecycle.js:92-115 (`probeProxyVersion`: resolves only on `res 'end'` or `req 'error'`), hooks/proxy-lifecycle.js:123-136 (`requestShutdown`: same shape)
- **Severity:** P2
- **Confidence:** high
- **Claim tag:** CONFIRMED — reproduced (scratchpad/probe-hang.mjs): a stub that writes the `/_status` head plus a partial body then destroys the socket leaves `probeProxyVersion(port)` STILL PENDING after 2500 ms; scratchpad/res-error-probe.mjs shows Node emits neither `'end'` nor an uncaught `'error'` on that response, only `'aborted'`/`'close'`
- **Failure trigger:** something accepts TCP on `PROXY_PORT`, answers `/_status` headers, and then dies or resets before the body completes — a wedged or crashing old proxy, exactly the process the version check exists to replace. The 1000 ms `timeout` option is a socket-INACTIVITY timer; a socket that is destroyed is not inactive, it is gone, so the timer never fires either.
- **Blast radius:** `ensureProxyRunning()` awaits a promise that never resolves → `hooks/session-start.js` never reaches `process.exit(0)` → hooks.json kills it at 10 s. The stale proxy is NOT replaced and no message is written (the hook is silent by design, IC13). Degraded-but-recoverable (the next session retries), and rare — but the case it fails on is the one it is for.
- **Evidence:** probe output `STILL PENDING after 2500ms 2507ms`; code read: the only `resolve()` calls are inside `res.on("end")` and `req.on("error")`.
- **Fix:** add `res.on("close", () => resolve(undefined))` in `probeProxyVersion` and `res.on("close", () => resolve(false))` in `requestShutdown`. `'end'` precedes `'close'` on a complete response, so the success path is unchanged (first `resolve` wins).
- **Guardrail:** two tests in `test/proxy-lifecycle.test.js` ("probeProxyVersion settles when the response is cut mid-body", "requestShutdown settles when the response is cut mid-body") that race the call against a 3 s timer and fail on the timer. Both red against the current code.

### [F08] Eight comments cite source by `file:line`; four of the eight are already wrong
- **Location:** scripts/bench-grades.js:395 (`src/models.js:162` → `REFRESHED_GRADES` is at :201), scripts/bench-grades.js:399 (`docs/OPERATIONS.md:87` → the restart note is at :107/:114), test/couplings.test.js:222-223 (`router.js:90`/`:96` → `rankRoutes` call is :215, predicate scan :220), test/fixtures/group-order-subprocess.mjs:10 and test/render-models.test.js:441 (`test/models.test.js:415` → that line is a `glm.baseUrl` assignment, not the subprocess pattern); scripts/probe-vendors.mjs:238 and test/models.test.js:541 are still approximately right
- **Severity:** P3
- **Confidence:** high
- **Claim tag:** CONFIRMED — each cited line read this run (`sed -n` output in the P1/P2 shell log); four point at unrelated code
- **Failure trigger:** any edit above the cited line. CLAUDE.md's own Traps section says a comment that states behaviour "rots like untested code, but louder"; a line number is the fastest-rotting claim a comment can make and the repo has no mechanism for it (doctests execute values, not locations).
- **Blast radius:** a successor following `src/models.js:162` to understand the boot-time grade read lands in `loadRefreshedGrades`'s catch block; following `router.js:90` lands in `stripVariantSuffix`'s JSDoc. Wrong-place reads are how a "fix" gets applied to the wrong function. No runtime impact.
- **Evidence:** grep `[A-Za-z_-]+\.(js|mjs|md):\d+` over src/scripts/hooks/bin/test → exactly 8 hits, all in comments; four verified stale by reading the target line.
- **Fix:** rewrite each citation to the SYMBOL it means (`REFRESHED_GRADES` in src/models.js, `rankRoutes()`/the predicate scan in `resolve()`, the "grades.json … startup only" row in OPERATIONS, the subprocess-per-HOME pattern in models.test.js), which survives line churn.
- **Guardrail:** `test/couplings.test.js` lock "no comment cites source by line number" — walks src/scripts/hooks/bin/test and fails on the `name.(js|mjs|md):NNN` pattern with a pointer to use a symbol name instead.

### [F09] Reader-facing docs give `anthropic/claude-opus-4` as the OpenRouter routing example — the one id family the code deliberately refuses to publish — and `.env.example` lists two of the six valid `DEFAULT_BACKEND` values
- **Location:** README.md:69, .env.example:22, skills/setup/SKILL.md:64 (the `anthropic/…` example); .env.example:254 (`"claude" or "glm"`)
- **Severity:** P3
- **Confidence:** high
- **Claim tag:** CONFIRMED — src/models.js:800-806 filters `id.startsWith("anthropic/")` out of discovery with the comment "reaching Claude through a reseller means paying per token for what the session's OAuth plan already covers … Invariants 3 and 4 exist to keep Claude traffic on the Claude route; publishing an aggregator's copy invites the opposite"; src/providers.js:160 accepts any provider id as `defaultId`, and SKILL.md:64 itself documents `openrouter|deepseek|qwen|lmstudio`
- **Failure trigger:** a user reads the setup skill or README, picks the first example, and sets a subagent's model to `anthropic/claude-opus-4` — routing Claude traffic through a metered reseller. The doc is the trigger; nothing in code stops it (by design — the id is valid on OpenRouter).
- **Blast radius:** money, silently: a Claude turn billed on OpenRouter credits instead of the OAuth plan, with a healthy-looking routing line (`anthropic/claude-opus-4 -> openrouter`). Docs contradicting the code's stated invariant reasoning is also how the reasoning gets "fixed" away by a later editor. The `DEFAULT_BACKEND` line is plain rot: four valid values undocumented at the one place a user sets it.
- **Evidence:** the three doc lines and the two source comments quoted above, all read this run.
- **Fix:** replace the example id with `deepseek/deepseek-v4-pro` (curated, graded, non-Claude) in all three docs; rewrite the `.env.example` `DEFAULT_BACKEND` comment to name every provider id and say a backend must be registered (keyed) to be the default.
- **Guardrail:** `test/couplings.test.js`: (a) no reader-facing doc offers an `anthropic/` id as an example; (b) the comment block above `DEFAULT_BACKEND=` in `.env.example` names every id in `PROVIDER_IDS` (read from src/providers.js, so a new provider joins the lock).

### [F10] `/v1/models` fetch legs report a timeout that lands mid-body as "invalid response shape"
- **Location:** src/models.js:618-623 (`fetchGlmModels`), :731-736 (`fetchQwenModels`), :781-786 (`fetchOpenRouterModels`), :852-857 (`fetchDeepSeekModels`) — the inner `try { body = await res.json() } catch { return { error: "invalid response shape" } }` swallows the AbortError before the outer `err.name === "AbortError"` branch can see it
- **Severity:** P3
- **Confidence:** high
- **Claim tag:** CONFIRMED — reproduced (scratchpad/abort-midbody.mjs): stub sends headers + `{"data":[` and stalls; `collectModels` with `modelsTimeoutMs: 200` returns `_errors: [{"provider":"glm","message":"invalid response shape"}]`
- **Failure trigger:** a vendor catalog endpoint that starts answering and then stalls past `modelsTimeoutMs` (3 s default) — a slow or overloaded vendor, which is the common shape of a real outage; an instant refusal is the rare one.
- **Blast radius:** diagnostic only: `/v1/models` still degrades correctly (200 with `_errors`, static fallback where one exists). But the operator reading `_errors` or `/cc-proxy:models`'s `! glm: invalid response shape` is sent to look for a schema change when the cause is latency. Same class as F07: a reported reason that is not the real one.
- **Evidence:** probe output above; the four inner catches read.
- **Fix:** in each inner catch, `if (err?.name === "AbortError") return { error: "timeout" };` before the shape error (or hoist `res.json()` out of the inner try and let the outer catch classify it).
- **Guardrail:** `test/models.test.js` case "a stall after the headers is reported as timeout, not invalid response shape" against a local stub, on the GLM leg (all four legs share the shape; one e2e case plus the code being identical is stated, not assumed — the fix touches all four).
- 2026-09-02 P2: self-verification pass — re-read each finding against the code; F06/F07/F10 hold with repro; F08's stale citations re-checked by `sed -n` (four wrong, two approximate); F09's contradiction is quoted from both files. Nothing dropped, nothing downgraded.
- 2026-09-02 P2: EXIT — dry pass: a second sweep of the P1 read set surfaced nothing above P3 not already listed. TRUNCATION (unchanged from P1): render-models.js 1–679, README/OPERATIONS/ARCHITECTURE grepped not line-read, most test files not line-read.
- 2026-09-02 P3: tests written FIRST, red against unfixed code: direct-run.test.js (module missing; then with the module present the e2e run of status.js through a symlinked "with space" path printed "" → red), proxy-lifecycle.test.js ×2 ("never settled — 'end' is not the only terminal event"), models.test.js ×1 (`invalid response shape` where `timeout` expected). Run: `node --test test/direct-run.test.js test/proxy-lifecycle.test.js test/models.test.js` → 105 tests / 101 pass / 4 fail.
- 2026-09-02 P3: F06 fixed — scripts/direct-run.js isDirectRun() (fileURLToPath + path.resolve + realpathSync both sides, every failure → false); guard replaced in status/list-models/bench-speed/bench-grades/render-models. F07 fixed — res.on("close") in probeProxyVersion (→ undefined) and requestShutdown (→ false). F10 fixed — the four inner catches classify AbortError as "timeout" first. F08 fixed — all eight citations rewritten to symbols. F09 fixed — README:69, .env.example:22 + DEFAULT_BACKEND comment, SKILL.md:64.
- 2026-09-02 P3: VERIFY — `pnpm check` GREEN: lint clean; 517 tests / 515 pass / 0 fail / 2 skipped. Baseline 504/502/0/2 → +13 (6 direct-run, 2 lifecycle, 1 models, 4 couplings), 0 regressions.
- 2026-09-02 P4: guardrails in test/couplings.test.js: "no script spells the direct-run guard as a raw file:// comparison", "no comment cites a source file by line number", "no reader-facing doc offers an anthropic/ id as the OpenRouter example", ".env.example's DEFAULT_BACKEND comment names every provider id". Detection proven for all four: the first two tripped on their own explanatory comments during development (rephrased), the last two mutation-verified RED (README example reverted → "README.md lines 69 give an anthropic/ id"; `lmstudio` dropped from the comment → "does not mention lmstudio") and restored → 19/19.
- 2026-09-02 P4: context transfer — CLAUDE.md: three new couplings rows + the buildProviders row extended + one Traps bullet ("A script that prints nothing and exits 0 may never have run"); CHANGELOG `[0.8.3] — unreleased` (3 Fixed, 2 Changed + audit-trail line). Version NOT bumped — a release decision; the release gate refuses a v0.8.3 tag until package.json/plugin.json move.
- 2026-09-02 P4: process slip logged: `git checkout -- README.md` used to undo a mutation ALSO reverted the uncommitted F09 README edit (same trap the prior run logged with refresh-lock.js); caught by the `grep` that followed, re-applied and verified at README:69. Lesson stands: restore from a saved copy, never from HEAD, while the tree is dirty.
- 2026-09-02 REVIEW: Copilot round 1 on PR #56 (6a564ef), 2 comments, both valid. (1) The direct-run lock regex matched only the backtick spelling → widened to any quote style, either operand order, and the `"file://" + argv[1]` concatenation; mutation-verified: four planted spellings in scripts/ all named by the failure. (2) The two symlink tests would fail on a Windows box without symlink permission → gated on a real capability probe (`{ skip: reason }`), never on process.platform; both still RUN here (ok, not skipped). `pnpm check` GREEN: 517 / 515 / 0 / 2.
- 2026-09-02 CORRECTION (maintainer commits 89b22ac, 2810baa on PR #56): F06's claim that "three sibling scripts (probe-vendors.mjs, release-gate.mjs, version-guard.js) already used the correct decoded comparison" was WRONG in the way that mattered. Decoding fixes a space/%/# and Windows; it does not fix a SYMLINK — import.meta.url is the main module's realpath, argv[1] is the shell's spelling — and the maintainer measured both release guards disarming through a symlinked checkout (version-guard: direct exit=1, symlink exit=0; release-gate: direct exit=2, symlink exit=0). The audit had the symlink mechanism in hand (it is in isDirectRun's own JSDoc) and did not re-apply it to the three scripts it had just called correct. All three now use isDirectRun(); a third couplings lock forbids the decoded-without-realpath spelling. Lesson for the ledger: "already uses the better form" is a claim about ONE failure mode unless every mode the fix names is re-checked at that site.
- 2026-09-02 CORRECTION (89b22ac): two structural gaps in this run's own fixes, both "fix applied at N sites, test at one": (a) the F10 AbortError→timeout classification was pinned on the glm leg only; the maintainer measured that openrouter/qwen swallow leg errors into the static fallback (so an _errors assertion there passes with the fix reverted), added a deepseek behavioural case and a couplings lock over all four legs; (b) the F06 couplings lock forbade the OLD spelling but not a wrong NEW call — `isDirectRun()` with no argument compares argv[1] to itself and runs main() on import at every site; now locked. Also (2810baa): a vendor RESET after the headers throws `TypeError: terminated`, not AbortError, so it still labelled "invalid response shape" with no log line; each leg now logs `[models] <provider> body read failed` with err.cause.
- 2026-09-02 VERIFY on the maintainer's head 2810baa: `pnpm check` GREEN — 522 tests / 520 pass / 0 fail / 2 skipped, lint clean. Release gate dry run on f7b7e26: `release gate OK: v0.8.3 == plugin.json == package.json == newest CHANGELOG heading`.
