{
  "name": "cc-proxy-plugin",
  "private": true,
  "type": "module",
  "version": "0.5.0",
  "description": "GLM, DeepSeek, OpenRouter, and Qwen proxy for Claude Code — stateless local router with manual /model switching and per-provider auth.",
  "license": "MIT",
  "packageManager": "pnpm@11.3.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "bin": {
    "cc-proxy": "./bin/cc-proxy.js"
  },
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "test": "node --test test/*.test.js",
    "check": "pnpm lint && pnpm test",
    "proxy": "node bin/cc-proxy.js",
    "models:html": "node scripts/render-models.js > docs/models.html",
    "sync-version": "node scripts/sync-version.mjs",
    "version": "node scripts/sync-version.mjs && git add .claude-plugin/plugin.json"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9"
  }
}
